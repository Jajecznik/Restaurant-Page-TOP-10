export default function contactTab() {
    const contentContainer = document.getElementById('content');

    const test = document.createElement('p');
    test.innerText = "CONTACT";
    contentContainer.appendChild(test);
}